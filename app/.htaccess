# Aplikacni interni soubory - zadny primy pristup z webu.
# (PHP include z filesystemu funguje dal; blokuje se jen HTTP pristup.)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
